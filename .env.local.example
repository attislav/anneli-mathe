# Copy this file to .env.local and fill in real values.
# .env.local is gitignored. NEVER prefix secrets with NEXT_PUBLIC_ —
# anything with that prefix is bundled into the client and visible to every visitor.

OPENAI_API_KEY=
